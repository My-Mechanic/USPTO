# Copy to .env for local runs. Do NOT commit .env.
#
# Port for the local bridge that the frontend's "Sync Now" calls.
PORT=8787
#
# There are intentionally NO USPTO credentials here. You sign in yourself in the
# browser window that opens (username + password + MFA). Nothing is scripted or
# stored except an optional local session cookie in ./.auth (git-ignored).
